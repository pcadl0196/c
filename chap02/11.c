#include <stdio.h>
//문자형 변수 이해하기(char)
//문자형 변수는 작은 범위으 ㅣ값을 저장하기 위한 변수형
main()
{
    char ch;
    unsigned char j;
    char k;

    ch = 200;
    j = 200;
    k = 'a';

    printf("문자형 변수 ch의 값은 %d \n", ch);
    printf("문자형 변수 j의 값은 %d \n", j);
    printf("문자형 변수 k의 값은 %d \n", k);
}