#include <stdio.h>//stdio.h란 파일을 포함하라는 뜻, 이 파일에는 화면에 메시지를 띄울수 있게 도와주는 여러가지 함수들이 포함되어있음

int main() {//main함수는 프로그램이 시작되는 함수임, {}는 몸체를 알려주는데 사용됨
    // printf("Hello, World! \n");//printf는 화면에 내용을 출력해주는 함수
    // printf("Hi, Computer \n");
    int a;
    a = 10;
    printf("a 의 값: %d \n", a);
    return 0;//0을 반환한다는의미

}